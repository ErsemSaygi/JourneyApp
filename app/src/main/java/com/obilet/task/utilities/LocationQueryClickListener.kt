package com.obilet.task.utilities

import android.view.View

/**
 * Interface that returns city selection result as view
 */
interface LocationQueryClickListener {
    fun onLocationClicked(v: View)
}